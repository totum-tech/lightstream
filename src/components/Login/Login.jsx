import React from 'react';
import { Button } from 'rebass';

const Login = () => (
  <div style={{ height: '100px' }}>
    Login
    <Button>
      Login
    </Button>
  </div>
);

export default Login;
